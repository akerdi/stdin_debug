{
  "name": "stdin_debug",
  "version": "1.0.0",
  "description": "nodejs 启动后调试不方便？试试stdinDebug。您要做的是`注册`，然后随时在控制台调用已注册的方法。如:",
  "main": "/lib/stdinDebug.ts",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "dev": "ENABLE_STDIN_DEBUG=true TS_NODE_FILES=true TS_NODE_TRANSPILE_ONLY=true ts-node --files -r tsconfig-paths/register main.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/akerdi/stdin_debug.git"
  },
  "keywords": [
    "stdin",
    "debug"
  ],
  "author": "akerdi",
  "license": "SEE LICENSE IN LICENSE",
  "bugs": {
    "url": "https://github.com/akerdi/stdin_debug/issues"
  },
  "homepage": "https://github.com/akerdi/stdin_debug#readme",
  "devDependencies": {
    "@types/node": "^17.0.38",
    "ts-node": "^8.10.2",
    "tsconfig-paths": "^3.10.1",
    "typescript": "^4.3.5"
  }
}
